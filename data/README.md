# Data

Raw audio files are not committed to GitHub. Place the local MLEnd homework dataset here:

```text
data/MLEndHWII_sample_800/
```

The reproducible pipeline scans only `data/MLEndHWII_sample_800/` when building metadata. The older 400-file sample is intentionally not used for corrected evaluation because it overlaps with the 800-file dataset.
